# NASDAQ Hunter — 产品需求文档 (PRD)

**版本**: v2.0  
**日期**: 2026年5月  
**状态**: 生产运行中（5年回测验证完成，V6.0 Production 参数锁定，全自动推送运行中）  
**通知渠道**: Lark Bot Webhook × 2 群组  

---

## 目录

1. [产品概述](#1-产品概述)
2. [SNDK 归因分析（模型基础）](#2-sndk-归因分析模型基础)
3. [SNDK DNA 模型设计](#3-sndk-dna-模型设计)
4. [历史回测结果（V6.0 Production）](#4-历史回测结果v60-production)
5. [系统架构](#5-系统架构)
6. [数据库设计（核心）](#6-数据库设计核心)
7. [功能需求详细说明](#7-功能需求详细说明)
8. [每日运营流程](#8-每日运营流程)
9. [Lark 消息卡片规范](#9-lark-消息卡片规范)
10. [测试与验收标准](#10-测试与验收标准)
11. [风险与局限性](#11-风险与局限性)
12. [版本规划](#12-版本规划)

---

## 1. 产品概述

### 1.1 产品愿景

基于对 SanDisk（SNDK）2025-2026 年 +3300% 爆发路径的深度归因分析，构建一套能够在类似机会爆发前**第一时间发出通知**的全自动量化扫描系统。用户每天北京时间 12:00 打开 Lark 即可看到清晰的"买入/等待"指令，无需任何手动操作。

### 1.2 核心价值主张

| 价值 | 说明 |
|------|------|
| 提前发现 | 平均领先 146 天（约5个月）发出信号，目标 >45 天 ✅ |
| 高精确率 | 5年回测（2021-2026）已结算信号精确率 71.4%，远超 50% 目标 ✅ |
| Alpha 超额 | 年化 +23.9% vs QQQ +16.3%，Alpha **+7.6%**，目标 >+5% ✅ |
| 全自动化 | 每天定时扫描 433+ 只股票，无需人工干预，北京时间 12:00 定时推送 |
| 零噪音 | 没有信号时静默推送，有信号时高优先级唤醒 |
| 可解释 | 每条建议附带 Claude AI 逐层分析过程和止损策略 |
| 数据自增 | 每日自动刷新价格/利率/基本面，历史数据库持续积累 |

### 1.3 目标用户

| 用户角色 | 核心需求 | 使用场景 |
|----------|---------|---------|
| 个人投资者（主） | 早于市场发现爆发股，明确的买入指令 | 每天北京时间 12:00 查看 Lark 日报 |
| 量化研究者 | 模型验证、回测数据、信号分析 | 深度分析历史数据与模型精确率 |
| 产品迭代者 | 添加新股票、调整阈值、扩展推送渠道 | 修改配置文件后重启服务 |

### 1.4 产品边界（明确不做的事）

- **不**提供自动化交易执行——所有买卖操作由用户手动完成
- **不**覆盖加密代理股（MSTR/COIN）和生物科技二元事件
- **不**承诺盈利——精确率 71.4% 意味着约每 10 次有 3 次止损，止损机制（-20%）是必要保护

---

## 2. SNDK 归因分析（模型基础）

SanDisk 是过去 5 年纳斯达克最典型的基本面驱动爆发案例，模型所有设计决策基于对其爆发路径的量化还原。

### 2.1 爆发路径时间轴

| 时间 | 事件 | 可量化信号 | 意义 |
|------|------|-----------|------|
| 2025.02 | 从 WDC 拆分上市 @$38 | 拆分解锁维度 | 估值被母公司 HDD 业务压制，首次独立定价 |
| 2025.04 | 恐慌低点 $27.89 | 52 周高低比 >2x | 关税担忧，模型此时无法触发（数据不足）|
| 2025.Q1 | 毛利率 16%→26% | 毛利率拐点 +10ppt | 第一个可测量信号，领先股价约 9 个月 |
| 2025.Q2 | 收入 +23% YoY，EPS 超预期 +321% | 连续超预期确认 | 趋势确认，机构开始建仓 |
| 2026.02.17 | 回测信号触发 $590.59 | DNA=80 综合=80 | 模型确认触发 |
| 2026.04.30 | 收入 +251%，毛利率 78%，纳入 S&P500 | 全面爆发确认 | 峰值 +164%（从触发价计） |
| 2026.05 | 全年 +3300%（从分拆价计）| — | 完整爆发周期结束 |

### 2.2 五大爆发驱动因素（按权重排序）

1. **收入加速（权重 26%，关键）** — 不是简单高增速，而是增速在加速：+12%→+23%→+61%→+251%
2. **毛利率拐点（权重 24%，关键）** — 从亏损期快速回升，单季跳升 >3ppt 触发额外加分
3. **连续 EPS 超预期（权重 22%，关键）** — 连续 2+ 季超预期 >5%，且幅度扩大
4. **拆分/重组解锁（权重 10%）** — 近 24 个月内上市且 52 周高低比 >1.5x
5. **叙事重定价（权重 10%）** — 分析师评级密集上调速度作为代理指标
6. **供需周期拐点（权重 8%）** — 成交量/均量比 + 当前价/52 周高

> **关键洞察**: 模型在回测中于 2026-02-17（$590.59）触发 SNDK 信号，此后峰值 +164%。这是精确率和召回率之间的有意取舍——宁可晚入，也要确认基本面。

---

## 3. SNDK DNA 模型设计（V6.0 Production 参数锁定）

### 3.1 总体架构：四层门控 + DNA 评分

模型采用**串联过滤逻辑**，必须通过全部四层门控才会生成买入信号。

| 层级 | 名称 | 核心规则 | 示例过滤 |
|------|------|---------|---------|
| L1 Quality | 质检 | 毛利率≥40%（超高速增长豁免）、收入增速 50-800%、价格 $5-$3000 | RIVN（毛利率-44%）、低基数周转股（增速>800%）|
| L2 Outlook | 预期 | 分析师下季营收预期 vs 当季实际，减速>40ppt 直接淘汰 | CELH 2024、PTON |
| L3 Macro | 环境 | Fed>5%→阈值88；Fed>4.5%→阈值83；其他→阈值70 | 高利率期提高门槛保护 |
| L4 Signal | 信号 | DNA≥72 + 趋势≥45 + 综合分≥阈值 + 历史≥3天 | 核心评分组合门控 |

**超高速增长豁免（HYPER_GROWTH）**：收入 YoY ≥ 80% 时，毛利率下限从 40% 降至 20%。

**史诗跃升豁免（EPIC_TURNAROUND）**：单季毛利率跳升 ≥20ppt 或收入加速 ≥80ppt，豁免 MIN_HIST（最少历史天数）和 REQUIRE_ACCEL 要求。

### 3.2 DNA 评分：7 维度加权模型

| 维度 | 权重 | 是否关键 | 最低通过分 | 计算逻辑 |
|------|------|---------|-----------|---------|
| 毛利率拐点 | 24% | ✅ | 40 | 当前毛利率水平分（5-100）+ 单季提升>3ppt 加 10 分 |
| 收入加速 | 26% | ✅ | 45 | 当前 YoY 增速基础分 + 当季>上季×1.1 时加速奖励 15 分 |
| 连续 EPS 超预期 | 22% | ✅ | 35 | 近 4 季超预期>5%计数（0-4 季满分 100）+ 幅度奖励 |
| 拆分/重组解锁 | 10% | — | 0 | 上市<18 个月且高低比>2x 得 90 分；<36 个月得 35 分 |
| 叙事重定价 | 10% | — | 0 | 分析师买入占比水平分 + 环比上调速度奖励 |
| 供需周期拐点 | 8% | — | 0 | 成交量/均量比 × 0.4 + 当前价/52 周高 × 0.6 |

> **惩罚机制**: 三个关键维度（毛利率/收入加速/连续超预期）任意一个低于最低通过分 → DNA 分 × 0.5

### 3.3 综合分计算公式

| 历史天数 | 综合分公式 | 说明 |
|---------|-----------|------|
| < 2 天 | DNA × 0.45 | 数据极少，大幅降权 |
| 2-4 天 | DNA × 0.60 + 趋势分 × 0.40 | 开始引入趋势因子 |
| ≥ 5 天 | DNA × 0.50 + 趋势分 × 0.50 | 历史充足，趋势分等权 |
| 前瞻减速 | 综合分 × (1 - 减速幅度/200) | 减速>25ppt 时自动降权 |

### 3.4 趋势评分算法

| 分项 | 满分 | 计算方式 |
|------|------|---------|
| 斜率得分 | 35 | 线性回归过去 10 天 DNA 序列的斜率，斜率>4 得 35 分 |
| 连续天数得分 | 30 | DNA≥55 的连续天数，≥7 天得满分 30 分 |
| 总涨幅得分 | 25 | 最新 DNA vs 10 天前 DNA 的绝对差值，>25 分得 25 分 |

### 3.5 触发条件（AND 逻辑，V6.0 Production）

```
综合分 ≥ 70（中性）/ ≥ 83（限制性 Fed>4.5%）/ ≥ 88（激进加息 Fed>5%）
AND DNA ≥ 72
AND 趋势分 ≥ 45
AND 历史数据 ≥ 3 天
AND 通过 L1 Quality / L2 Outlook / L3 Macro 全部门控
AND 收入增速加速（当季 YoY > 上季 YoY × 1.15）  ← 最关键门控
```

### 3.6 置信度分级与仓位建议

| 等级 | 综合分范围 | 仓位建议 | 含义 |
|------|---------|---------|------|
| 🔴 STRONG（强）| comp ≥ 85 | 60-80% of capital | 绝对不可错过，高确定性机会 |
| 🟡 MEDIUM（中）| 75 ≤ comp < 85 | 20-40% of capital | 有把握，主动参与 |
| 🟢 WEAK（弱）| comp < 75（≥阈值）| 5-15% of capital | 参与性，严格快止损（-10%）|

### 3.7 止损策略（V6.0 Production）

- **固定止损**: 入场价 × 0.80（-20%，收盘价触发）；WEAK 信号用 -10% 快止损
- **收盘价判断**: 使用日收盘价（非盘中低点），避免盘中假突破
- **追踪止损**: 持仓盈利达 +50% 后，止损价提升至 `max(入场价, 最高收盘价 × 0.75)`，标记为 PROFIT_LOCK 并计入 WIN
- **无例外**: 无论市场条件如何，触及止损即执行

---

## 4. 历史回测结果（V6.0 Production，5年真实验证）

> **数据说明**: 走步回测（Walk-Forward），严格 Point-in-Time（`filing_date ≤ current_date`），无前视偏差。V6.0 为全局最优版本，参数已锁定。

### 4.1 North Star 指标

| 指标 | 数值 | 目标 | 状态 |
|------|------|------|------|
| 回测周期 | 2021-04-19 → 2026-05-18（1852天）| — | — |
| 策略年化收益 | **+23.9%** | — | — |
| QQQ 同期年化 | +16.3% | — | — |
| **Alpha（超额收益）** | **+7.6%** | >+5% | ✅ |
| 模拟组合终值 | $296,655（初始 $100,000）| — | +196.7% |

### 4.2 P0 生死线指标（基于 31 条实际入场交易）

| 指标 | 数值 | 目标 | 状态 |
|------|------|------|------|
| **精确率** | **71.4%**（WIN 10条 / 已结算 14条）| ≥50% | ✅ |
| **真实误判率（TER）** | **21.4%**（STOP_WRONG 3条 + STOP_CRASH 1条）| <30% | ✅ |
| 市场崩跌判定 | QQQ 7日跌>5% 或 VIX>30 时的 STOP 归为 CRASH（非误判）| — | — |

### 4.3 P1 质量指标

| 指标 | 数值 | 目标 | 状态 |
|------|------|------|------|
| **平均领先量** | **146 天**（18条信号达到+50%里程碑 / 共31条）| >45天 | ✅ |
| 同类优质召回率 | 6.0%（命中12只 / L1达标100%+宇宙200只）| >15% | ❌ |
| 全局遗漏率 | 遗漏 946只（精确率优先策略的固有代价）| — | 有意取舍 |

> **召回率说明**: 召回率 6.0% 是系统性权衡的结果，非技术缺陷。模型的核心原则是"宁缺毋滥"——允许遗漏爆发股，绝不容忍持续误判。历史证明此策略正确：V7.0/V8.0 两次尝试提高召回率，均导致 Alpha 崩溃（V7.0: +4.6%，V8.0: -5.2%），V6.0 确认为全局最优。

### 4.4 信号分布与组合动态

```
总信号: 37条  实际入场: 31条  已结算: 14条  持仓中: 17条

年度分布: 2022:15  2023:1  2024:3  2025:3  2026:15
结果分布: ✅WIN 10  🔒LOCK 9  🛑STOP_WRONG 3  💥STOP_CRASH 1  ❌LOSS 0  ⏳OPEN 17
```

**组合模拟规则（V6.0 Capital-Driven）**：
- 无持仓上限：资金充足即可入场，不设最大持仓数
- 分级仓位：STRONG=40% NAV，MEDIUM=20% NAV，WEAK=10% NAV
- 层级抢占：新 STRONG 信号可驱逐最差 WEAK→MEDIUM；新 MEDIUM 可驱逐最差 WEAK
- 现金收益：闲置现金按 5% APY 计算日收益

### 4.5 关键信号样本

| 股票 | 入场日 | 入场价 | DNA | 综合 | 峰值% | 结果 |
|------|-------|-------|-----|------|-------|------|
| HIMS | 2022-11-08 | $5.53 | 100 | 78 | +118% | 🔒LOCK |
| NET | 2022-11-04 | $41.09 | 90 | 70 | +109% | 🔒LOCK |
| ACMR | 2022-11-08 | $8.44 | 82 | 70 | +138% | 🔒LOCK |
| DCTH | 2024-05-14 | $7.07 | 100 | 100 | +156% | 🔒LOCK |
| NVDA | 2024-05-29 | $114.76 | 100 | 100 | +51% | ✅WIN |
| NUTX | 2025-03-31 | $47.03 | 82 | 82 | +305% | 🔒LOCK |
| SNDK | 2026-02-17 | $590.59 | 80 | 80 | +164% | ⏳OPEN |

### 4.6 参数迭代历史（V6.0 是全局最优）

| 版本 | 关键变化 | Alpha | 结论 |
|------|---------|-------|------|
| V5.0 | 置信度分级、层级抢占、快止损 | — | 基础架构奠定 |
| **V6.0** | 废除持仓上限、完整层级抢占、deployed_keys 度量 | **+7.6%** | ✅ 全局最优，参数锁定 |
| V7.0（废弃）| 提高阈值 70→72、加 MAX_DAILY_TRADES=2 | +4.6% | ❌ 阈值不消灭误判，节流导致赢家变输家 |
| V8.0（废弃）| 60天行业ETF动量门 | -5.2% | ❌ 滞后指标在熊市底部封杀全部信号 |

---

## 5. 系统架构

### 5.1 整体数据流

```
Finnhub API  ──┐
yfinance     ──┤──→ nasdaq_downloader.py ──→ nasdaq_history.db
               │     (初始下载 + 每日增量)         │
               │                                   │
               └──→ scanner_v2.py ────────────────→┘
                     (每日17:30 EST扫描)             │
                         │                          │
                         ▼                          │
               四层门控 + DNA评分                    │
                         │                          │
                         ▼                          │
               Claude AI 报告生成                   │
                         │                          │
                         ▼ (存 DB，不立即推送)       │
                    reports 表                       │
                         │                          │
            北京时间 12:00 统一推送                  │
                         │                          │
               ┌─────────┴──────────┐               │
          Lark 群 #1            Lark 群 #2           │
   (FEISHU_WEBHOOK)      (FEISHU_WEBHOOK_2)          │
```

### 5.2 调度时间线（每日）

| 北京时间 | UTC | 美东时间 | 事件 |
|---------|-----|---------|------|
| 05:30（夏令）/ 06:30（冬令）| 21:30/22:30 | 17:30 EST/EDT | **扫描触发**：DB增量刷新 → 全市场扫描 → 报告存DB |
| **12:00** | **04:00** | 00:00/23:00 | **推送触发**：读DB最新报告 → 同时推送两个Lark群 |

### 5.3 模块职责

| 文件 | 职责 | 优先级 |
|------|------|-------|
| `nasdaq_downloader.py` | 历史初始化 + `daily_refresh()`每日增量（价格/利率/基本面）| P0 |
| `historical_backtest.py` | 5年走步验证，V6.0 Production 回测引擎 | P0 |
| `scanner_v2.py` | 每日扫描 + DNA评分 + Claude报告 + `push_pending_report()` | P0 |
| `push_channels.py` | Lark Card 2.0 卡片推送（双群组）+ Discord | P0 |
| `scheduler_v2.py` | 双触发调度：17:30 EST 扫描 + 04:00 UTC 推送 | P0 |
| `run_scheduler.sh` | 调度器启动脚本（环境变量加载）| P0 |
| `~/Applications/NasdaqHunter.app` | macOS Login Item，开机自启 | P0 |
| `data/nasdaq_history.db` | 主数据库（历史+增量统一存储）| P0 数据 |

### 5.4 API 依赖

| API | 用途 | 限制 | 关键端点 |
|-----|------|------|---------|
| Finnhub（免费）| 股票列表、季报、EPS、指标、内部人交易、分析师 | 60次/分钟 | `/stock/earnings`, `/stock/metric` |
| yfinance（免费）| 日线 OHLCV 历史价格 + 每日增量 | 无限制 | 批量下载 |
| Anthropic Claude | 生成自然语言报告和投资建议 | 按 token 计费 | claude-sonnet-4-6 |
| Lark Webhook × 2 | 推送消息卡片到两个用户群 | 无限制（自有）| open.larkoffice.com |

### 5.5 自动化启动机制（macOS，双保险）

| 机制 | 实现 | 作用 |
|------|------|------|
| **Login Items** | `~/Applications/NasdaqHunter.app` | 用户登录后自启，继承完整 TCC 权限 |
| **crontab @reboot** | `crontab -l` | 系统重启后 15 秒备用启动 |
| 崩溃重启 | `scheduler_v2.py` 自身守护进程逻辑 | 信号/异常后优雅退出，外部重启兜底 |

---

## 6. 数据库设计（核心）

### 6.1 设计原则

> **核心原则**: `nasdaq_history.db` 是**唯一主数据库**，同时服务于：
> 1. 历史批量初始化（`nasdaq_downloader.py`）
> 2. 每日增量自动刷新（`scheduler_v2.py` 调用 `daily_refresh()`）
> 3. 走步回测分析（`historical_backtest.py`）
> 4. 每日扫描趋势积累（`daily_signals` 表记录 DNA 历史）
> 5. 报告存档与定时推送（`reports` 表，扫描存入，12:00 推出）

所有表使用 `INSERT OR REPLACE` 保证幂等性。

### 6.2 表结构全览

#### `prices` — 日线价格

```sql
CREATE TABLE prices (
    ticker  TEXT NOT NULL,
    date    TEXT NOT NULL,   -- YYYY-MM-DD
    open    REAL, high REAL, low REAL, close REAL, volume REAL,
    PRIMARY KEY (ticker, date)
);
```

**每日增量**：`daily_refresh()` 下载最近 5 天价格，`INSERT OR REPLACE` 覆盖更新。

#### `fundamentals` — 季度基本面（Point-in-Time）

```sql
CREATE TABLE fundamentals (
    ticker        TEXT NOT NULL,
    period        TEXT NOT NULL,   -- 财季末日期 YYYY-MM-DD
    filing_date   TEXT NOT NULL,   -- SEC 实际披露日（PIT 关键字段）
    revenue       REAL,
    revenue_yoy   REAL,
    gross_margin  REAL,
    eps_actual    REAL,
    eps_estimate  REAL,
    eps_surprise  REAL,
    beats_4q      INTEGER,
    PRIMARY KEY (ticker, period)
);
```

> **关键字段** `filing_date`：回测查询必须使用 `filing_date <= current_date`，防止前视偏差。  
> **每日滚动刷新**：`daily_refresh()` 每天刷新最旧的 200 只 ticker 基本面，3315 只约 17 天一轮（季报3个月一次，远超需求）。

#### `daily_signals` — 每日 DNA 评分（趋势积累核心）

```sql
CREATE TABLE daily_signals (
    ticker      TEXT NOT NULL,
    scan_date   TEXT NOT NULL,
    dna         INTEGER,
    trend       INTEGER,
    comp        INTEGER,
    threshold   INTEGER,
    triggered   INTEGER DEFAULT 0,
    price       REAL,
    stop_price  REAL,
    fed_rate    REAL,
    gm          REAL,
    rev_yoy     REAL,
    eps_sur     REAL,
    beats_4q    INTEGER,
    fail_reason TEXT,
    source      TEXT DEFAULT 'scanner',
    PRIMARY KEY (ticker, scan_date)
);
```

> `daily_signals` 是趋势评分的数据源。`load_history()` 读取最近 60 天记录计算趋势分，数据积累越多，趋势判断越准确。

#### `reports` — Claude AI 每日报告存档（扫描存入，12:00 推出）

```sql
CREATE TABLE reports (
    report_date  TEXT PRIMARY KEY,
    action       TEXT,              -- 'BUY' / 'WAIT' / 'ERROR'
    json         TEXT,              -- 完整 JSON（含 analysis_process、picks 等）
    pushed       INTEGER DEFAULT 0, -- 0=待推送，1=已推送
    created_at   TEXT
);
```

> **推送解耦**：扫描完成后报告写入 `pushed=0`；`push_pending_report()` 在北京时间 12:00 读取最新未推报告，推送后更新 `pushed=1`。

#### `macro_rates` — 宏观利率历史

```sql
CREATE TABLE macro_rates (
    date     TEXT PRIMARY KEY,
    fed_rate REAL
);
```

#### `scan_log` / `signal_outcomes` / `dl_progress` — 运营辅助表

（结构不变，详见代码注释）

### 6.3 每日数据增量规则

| 数据类型 | 更新方式 | 触发时机 | 数据量 |
|---------|---------|---------|-------|
| `prices` | `daily_refresh()`，最近5天，INSERT OR REPLACE | 每日扫描前 | ~3315 只 × 5天 |
| `macro_rates` | `download_macro()`，全量，INSERT OR IGNORE | 每日扫描前 | ~月度1条 |
| `fundamentals` | 滚动刷新最旧200只 | 每日扫描前 | 200只/天，17天完整覆盖 |
| `daily_signals` | 扫描写入当日评分 | 每日扫描后 | ~350只/天 |
| `reports` | 扫描写入，12:00 标记 pushed=1 | 扫描后/推送后 | 1条/天 |

---

## 7. 功能需求详细说明

### F01: 历史数据初始化（`nasdaq_downloader.py`）

| 属性 | 说明 |
|------|------|
| 功能 | 一次性下载 5 年全 NASDAQ 历史数据（价格 + 基本面 + 宏观利率）|
| 触发 | 手动运行 `python3 nasdaq_downloader.py`（仅首次）|
| 宇宙 | 3,315 只股票（scanner_v2 宇宙 + Finnhub 全量 NASDAQ 普通股）|
| 价格 | yfinance 批量下载，50 只/批，约 15-20 分钟 |
| 基本面 | yfinance 季度财报 + Finnhub EPS，约 90-120 分钟 |
| 断点续传 | `dl_progress` 表记录进度，中断后重启自动跳过已完成 |

### F02: 每日增量数据刷新（`nasdaq_downloader.daily_refresh()`）

| 属性 | 说明 |
|------|------|
| 功能 | 每次扫描前自动刷新最新数据，无需手动触发 |
| 价格更新 | yfinance 下载最近 5 天 OHLCV，~2-3 分钟 |
| 利率更新 | Finnhub FEDFUNDS 端点，秒级完成 |
| 基本面更新 | 滚动刷新最旧 200 只，每天 ~4 分钟，17 天完整覆盖 |
| 调用方 | `scheduler_v2._refresh_db()` 在扫描前自动调用 |

### F03: 每日自动扫描（`scanner_v2.run_scan()`）

| 属性 | 说明 |
|------|------|
| 触发时间 | 美东 17:30 EST/EDT（自动夏令时切换）|
| Phase 1 | 价格初筛 433+ 只，约 5-8 分钟 |
| Phase 2 | 深度评分 ~350 只，约 50-60 分钟（顺序执行，稳定无限流）|
| 报告生成 | Claude claude-sonnet-4-6，~15-30 秒 |
| **扫描后行为** | 报告存入 `reports` 表（`pushed=0`），**不立即推送** |
| 手动触发 | `python3 scanner_v2.py --force` |

### F04: 定时推送（`scanner_v2.push_pending_report()`）

| 属性 | 说明 |
|------|------|
| **推送时间** | **每天北京时间 12:00（UTC 04:00）**，固定不变 |
| 推送内容 | DB 中最新 `pushed=0` 报告；若已全部推过则重推最新一条 |
| 推送目标 | Lark 群 #1（FEISHU_WEBHOOK）+ Lark 群 #2（FEISHU_WEBHOOK_2）同时推送 |
| 手动触发 | `python3 scanner_v2.py --push-pending` |
| 容错 | 失败最多重试 3 次，间隔 1 分钟 |

### F05: 定时调度（`scheduler_v2.py`）

| 属性 | 说明 |
|------|------|
| 双触发 | 扫描（17:30 EST/EDT）+ 推送（04:00 UTC）两个独立触发时间 |
| 周末跳过 | 扫描和推送均跳过周六/周日 |
| 启动补偿 | 重启时检查：错过扫描→立即补扫；错过推送→立即补推 |
| 进程守护 | macOS Login Items 开机自启；crontab @reboot 备用 |
| 失败重试 | 扫描最多3次×5分钟间隔；推送最多3次×1分钟间隔 |

### F06: 历史走步验证（`historical_backtest.py`）

| 属性 | 说明 |
|------|------|
| 输入 | `nasdaq_history.db`（5年历史数据）|
| 模拟 | 逐日走步，严格 `filing_date <= current_date`（零前视偏差）|
| 组合模拟 | Capital-Driven（无持仓上限）+ 层级抢占 + 追踪止损 |
| 输出 | Alpha vs QQQ、精确率、TER、领先量、同类召回率 |
| 输出文件 | `data/backtest_report_3yr.txt` + `data/backtest_signals.json` |

### F07: Lark 消息卡片推送（`push_channels.py`）

| 推送类型 | 触发条件 | 卡片颜色 | 内容 |
|---------|---------|---------|------|
| BUY 信号 | action="BUY" | 红色 🔴 | 扫描统计、分析过程、每只标的（置信度+仓位建议+交易计划+逻辑）、Yahoo Finance 链接 |
| WAIT 日报 | action="WAIT" | 蓝色（wathet）| 核心指标 column_set（Fed/阈值/扫描数）、扫描结论、Watchlist 子弹列表、操作建议 |
| 错误报告 | 扫描失败 | 橙色 🟠 | 错误描述、已扫描数、建议操作 |

---

## 8. 每日运营流程

### 8.1 全自动运行时间线

```
北京时间 05:30（夏令）/ 06:30（冬令）
         │
         ▼ 扫描触发
  ① 数据库增量刷新（~10分钟）
     ├─ yfinance: 最近5天价格 → prices 表
     ├─ Finnhub: Fed Funds Rate → macro_rates 表
     └─ yfinance+Finnhub: 200只基本面滚动刷新 → fundamentals 表
         │
  ② 全市场扫描（~60-70分钟）
     ├─ Phase 1: 433只价格初筛 (~8分钟)
     ├─ Phase 2: ~350只深度评分 (~60分钟，10秒/只顺序执行)
     ├─ 四层门控 (L1→L2→L3→L4)
     └─ Claude 生成报告 (~30秒)
         │
  ③ 报告存入 DB（pushed=0，暂不推送）
         │
北京时间 12:00  ← 用户收到推送的时间（固定）
         │
  ④ 定时推送
     ├─ 读取 DB 最新未推报告
     ├─ push_feishu → Lark 群 #1
     └─ push_feishu → Lark 群 #2
         │
         ▼
  ⑤ 用户在 Lark 查看 BUY/WAIT 日报
```

### 8.2 手动操作命令速查

```bash
# 查看调度器是否运行
pgrep -fl scheduler_v2

# 重启调度器
pkill -f scheduler_v2
nohup bash ~/Documents/Nasdaq_Hunter/run_scheduler.sh &

# 立即手动推送今日报告（不等 12:00）
python3 scanner_v2.py --push-pending

# 强制重新扫描
python3 scanner_v2.py --force

# 测试推送格式
python3 scanner_v2.py --push-test

# 运行历史回测
python3 historical_backtest.py

# 实时查看日志
tail -f ~/Documents/Nasdaq_Hunter/data/scheduler.log
```

### 8.3 年度维护操作

| 操作 | 命令 | 频率 |
|------|------|------|
| 扩充至 10 年 | `python3 nasdaq_downloader.py --years 10` | 约 2 年后 |
| 重跑历史验证 | `python3 historical_backtest.py` | 每季度 |
| 宇宙更新 | 修改 scanner_v2.py TICKERS，重启 scheduler | 按需 |

---

## 9. Lark 消息卡片规范

### 9.1 BUY 信号卡片（红色，穿透勿扰）

```
🚨 NASDAQ Hunter · 2026-05-17 买入信号
━━━━━━━━━━━━━━━━━━━━━━
[column_set: 扫描股票 N只 | Quality通过 N只 | Signal触发 N只]
[column_set: Fed Rate X% | 触发门槛 ≥70 | 止损纪律 -20%]
━━━━━━━━━━━━━━━━━━━━━━
📊 分析过程（Quality → Outlook → Macro → Signal）
✅ L1质检 N只通过；✅ L2预期 N只；📊 宏观阈值=70（中性）
🎯 最强信号 TICKER DNA=84 趋势=67 综合=79 ✅
💡 决策：1只触发，置信度强，建议重仓介入
━━━━━━━━━━━━━━━━━━━━━━
📈 标的：TICKER  ⭐ 置信度：🔴 强（建议仓位 60-80%）参考 70%

🎯 交易计划
• 入场价：$XX.XX
• 防守止损：$XX.XX（收盘 -20% 击穿离场）
• 预期目标：+XXX%

🧠 核心逻辑
• 基本面：...
• 催化剂：...
• 风险提示：...
━━━━━━━━━━━━━━━━━━━━━━
📝 市场总结
⚠️ 本信号由量化模型生成，仅供参考。止损纪律（收盘-20%）无例外执行。
```

### 9.2 WAIT 日报卡片（蓝色，静默）

```
📊 NASDAQ Hunter · 2026-05-17 每日日报
━━━━━━━━━━━━━━━━━━━━━━
[column_set: Fed Rate X%（中性）| 触发门槛 综合分≥70 | 扫描股票 共N只]
━━━━━━━━━━━━━━━━━━━━━━
📋 扫描结论
今日 N只通过 Quality 质检，但均未达到 Signal 触发线（≥70分）。
[wait_reason]
[analysis_process 逐行要点]
━━━━━━━━━━━━━━━━━━━━━━
👁️ 重点关注 (Watchlist)
• TICKER | 综合分: N | 距阈值: ±N | 状态: ...
━━━━━━━━━━━━━━━━━━━━━━
💡 操作建议
• 当前无触发信号，保持空仓或持有现有仓位
• 关注 Watchlist 中综合分接近阈值的标的
• 止损纪律：收盘跌破入场价 -20% 立即离场，无例外
━━━━━━━━━━━━━━━━━━━━━━
📝 市场总结
```

**关键设计原则**：
- Feishu markdown **不支持** `|---|---|` 表格语法，Watchlist 一律用子弹列表 `•`
- 指标展示用 `column_set`（3列灰底背景块）
- BUY 卡颜色 `red`，WAIT 卡颜色 `wathet`，错误卡颜色 `orange`

---

## 10. 测试与验收标准

### 10.1 数据层验收

| 验收项 | 标准 | 验证方式 |
|-------|------|---------|
| 价格数据完整性 | NVDA/AMD/AAPL 5 年数据无缺失 | SQLite 查询记录数 |
| 基本面 PIT 有效性 | `filing_date > period`（披露日晚于财季末）| 抽样验证 |
| 每日增量 | 每次调度后 prices 表有新记录 | 日期查询 |
| 基本面滚动 | fundamentals 最大 filing_date 持续更新 | 日期查询 |

### 10.2 模型层验收

| 验收项 | 标准 | 验证方式 |
|-------|------|---------|
| V6.0 回测 Alpha | ≥ +5% | `python3 historical_backtest.py` |
| 精确率 | ≥ 50%（已结算）| 同上 |
| 真实误判率 | < 30% | 同上 |
| 领先量 | > 45 天 | 同上 |

### 10.3 系统层验收

| 验收项 | 标准 | 验证方式 |
|-------|------|---------|
| 北京时间 12:00 推送 | 每个工作日精确推送，±2 分钟内 | 观察 Lark 群 |
| 双群推送 | 两个群同时收到相同消息 | 对比群消息时间戳 |
| 推送格式 | BUY/WAIT 卡片无 `\|---|---|` 原始文本 | `--push-test` 验证 |
| 开机自启 | 重启 Mac 后调度器自动恢复 | 重启后 `pgrep -fl scheduler_v2` |
| 扫描-推送解耦 | 扫描完成不立即推送，reports.pushed=0 | `sqlite3` 查询 |

---

## 11. 风险与局限性

### 11.1 模型系统性局限

| 局限类型 | 描述 | 缓解措施 |
|---------|------|---------|
| 加密代理股 | MSTR/COIN 由 BTC 价格驱动，永远漏判 | 有意设计边界 |
| 生物科技二元事件 | CVKD/SLNO 由临床数据驱动，框架外 | 同上 |
| 低召回率（6%）| 精确率优先策略固有代价，每年 ~2-3 个信号 | 可接受权衡 |
| 市场系统性崩跌 | V-bottom 底部买入往往被止损（-20%）后再反弹 | STOP_CRASH 独立统计，不计入 TER |
| Finnhub 数据质量 | 免费版部分小盘股数据不完整 | 过滤价格<$5 |
| 趋势分冷启动 | 前 3 天精确率略低 | 历史数据库持续积累缓解 |

### 11.2 运营风险

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| Mac 电源关闭 | 扫描/推送无法执行 | Login Items + crontab @reboot 保证上电即恢复 |
| Finnhub 限流 | Phase 2 耗时延长 | 10秒/只顺序执行，在60次/分钟限额内 |
| Claude API 超时 | 报告生成失败 | fallback ERROR 报告自动存入 DB |
| Lark Webhook 失效 | 推送失败 | scheduler 保留3次重试，错误卡推送提示 |

### 11.3 重要免责声明

> 本系统所有输出内容（包括 Lark 推送的买入建议）**仅供参考，不构成任何投资建议**。股票投资存在亏损风险，过往表现不代表未来结果。用户应自行承担所有投资决策的后果。

---

## 12. 版本规划

| 版本 | 里程碑 | 核心功能 | 状态 |
|------|-------|---------|------|
| v1.0 | MVP 上线 | DNA 模型 + 四层门控 + Lark 推送 + 基础回测 | ✅ 完成 |
| v1.1 | 数据层完善 | 5年历史 + nasdaq_history.db + 走步验证 | ✅ 完成 |
| v1.2 | 模式统一 | 统一数据库 Schema，历史+增量一体化 | ✅ 完成 |
| v1.3 | 模型调优 | 756组参数网格搜索，精确率 15.8%→71.4%；三档置信度；分析过程推送 | ✅ 完成 |
| **v2.0** | **全自动化生产** | **V6.0 参数锁定（Alpha +7.6%）；推送-扫描解耦（12:00 北京推送）；双群推送；每日DB自动刷新；macOS 开机自启；WAIT 卡片 column_set 重构** | ✅ **当前版本** |
| v2.1 | 高DNA快触发 | DNA≥85 时降低 MIN_HIST 从3→1，捕获 ALAB/HIMS 类快速突破 | 下一步 |
| v2.2 | 市场状态感知 | VIX 集成，高波动期（VIX>25）自动提示降仓，减少系统性回调误判 | 下一步 |
| v3.0 | 多子模型 | 困境反转子模型 + 新股/IPO 专项追踪器 | 持续迭代 |

### 12.1 已知技术债

- Finnhub 免费版季报 `filing_date` 为估算值（季末+45天），非真实 SEC 披露日
- 趋势分依赖每日积累，数据库新增标的前 3 天精确率略低
- Claude 报告生成约需 15-30 秒，偶发超时需要重试
- Lark Webhook 只在有互联网连接时有效，程序须在能访问 larkoffice.com 的网络环境运行
- yfinance 不提供 PIT 数据（仅用于价格，不影响基本面严格性）

---

*文档版本 v2.0 | 最后更新 2026-05-18 | NASDAQ Hunter Project*
